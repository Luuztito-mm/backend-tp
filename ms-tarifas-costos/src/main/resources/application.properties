# puerto de este micro
server.port=8085
spring.application.name=ms-tarifas-costos

# ====== DATASOURCE (PostgreSQL) ====== cambia:localhost:5432
spring.datasource.url=jdbc:postgresql://localhost:5432/ms_tarifas_costos
spring.datasource.username=postgres
spring.datasource.password=Nc131989@
spring.datasource.driver-class-name=org.postgresql.Driver


# ====== JPA / HIBERNATE ======
# como ya tenés la BD creada, podés dejar none
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ================== MICROS EXTERNOS ==================

# usamos ms-rutas-tramos para pedir la distancia
app.ms-rutas-tramos.url=http://localhost:8084
# (cuando esté en Docker, esto después será algo tipo:
#  app.ms-rutas-tramos.url=http://ms-rutas-tramos-grupo7:8084)
