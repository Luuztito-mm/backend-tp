# URL del servidor Keycloak
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8088/realms/bda-realm
spring.security.oauth2.resourceserver.jwt.claims-role-prefix=
=======
# puerto de este micro
server.port=8083
spring.application.name=ms-depositos

# ====== DATASOURCE (PostgreSQL) ====== se cambia esta parte: localhost:5432
spring.datasource.url=jdbc:postgresql://host.docker.internal:5432/ms_depositos
spring.datasource.username=postgres
spring.datasource.password=641723
spring.datasource.driver-class-name=org.postgresql.Driver

# ====== JPA / HIBERNATE ======
# como ya tenés la BD creada, podés dejar none
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
