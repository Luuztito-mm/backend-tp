# puerto de este micro
server.port=8081
spring.application.name=ms-clientes-solicitudes

# ====== DATASOURCE (PostgreSQL) ====== se cambia esto: postgresql://localhost:5432/ms_c
spring.datasource.url=jdbc:postgresql://host.docker.internal:5432/ms_clientes_solicitudes
spring.datasource.username=postgres
spring.datasource.password=641723
spring.datasource.driver-class-name=org.postgresql.Driver

# ====== JPA / HIBERNATE ======
# como ya tenés la BD creada, podés dejar none
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

server.error.include-message=always
server.error.include-stacktrace=always
